@model HomeViewModel

@await Html.PartialAsync("_Carousel")

<h2>Os lanches preferidos são</h2>

<div class="row">
    @foreach (var lanche in Model.LanchesPreferidos)
    {
        @await Html.PartialAsync("_LanchesResumo", lanche);
    }
</div>